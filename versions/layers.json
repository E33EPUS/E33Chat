{
  "_comment": [
    "共享层的声明。【一个层就是对目标的一个谓词】：满足它的目标，才把这一层的代码编进去。",
    "",
    "【三个轴】",
    "  since      钉住版本：Minecraft 版本不低于它的目标才挂",
    "  loader     钉住加载器：这个加载器的目标才挂",
    "  mappings   钉住映射家族：用这套映射名的目标才挂",
    "至少写一个。一个都不写就是「全部目标」，那是 shared/，不是层。",
    "",
    "【禁哪个轴 = 哪个轴没被钉住】",
    "  只写 since            版本层：该批版本、各加载器共用      → 层里不许有绑加载器或绑映射的东西",
    "  只写 loader           加载器层：该加载器、各版本共用      → 层里不许有绑版本或绑映射的东西",
    "  只写 mappings         映射层：该映射家族、各加载器共用    → 层里不许有绑版本或绑加载器的东西",
    "  写了多个轴            交集层：两个/三个轴都钉住，都不禁",
    "",
    "【目录是推出来的，不是声明的】",
    "  只写一个轴 → layers/<轴名>/<层名>/      例如 layers/version/1.21+、layers/mapping/official",
    "  写了多个轴 → layers/<轴名按字母序用 + 连接>/<层名>/",
    "声明得出来、编不过去的那种矛盾，就是靠「目录由声明推出来」消掉的 —— 没有第二处可以互相矛盾。",
    "",
    "【层名是「这套 API 从哪个 Minecraft 版本开始有」/「哪套映射」，不是某一个具体版本】",
    "1.21.1 与将来的 1.21.x / 更高版本都挂 1.21+，因为它们装的是同一份代码。",
    "",
    "【populated：这一层现在该不该有内容】",
    "true 时层里一个文件都没有就红；声明为 false 却扫到内容也红。层可以先声明后填，",
    "但「空」要是显式声明的状态，不能默认容忍。",
    "",
    "【层由编译证明，不由眼看】映射层的正文来自「Neo 与 Forge 逐字相同、只有 Fabric 不同」",
    "那一批 —— 它们本来就在两个 Minecraft 版本上各自编译通过，版本中立是证得的。"
  ],

  "official": {
    "mappings": "official",
    "populated": false,
    "note": "映射层：Forge / NeoForge 共用的 Mojang 官方名代码。Fabric 走 Yarn，不挂这层，自己在平台目录保留同路径孪生副本。wave-2 填充时把 populated 翻成 true。"
  },

  "1.21+": {
    "since": "1.21",
    "populated": false,
    "note": "版本层：1.21 起才有的原版 API。1.20.1 不挂载这一层。待填。"
  },

  "forge": {
    "loader": "Forge",
    "populated": false,
    "note": "加载器层：Forge 各版本共用。当前两个 Forge 目标分属 1.20.1 / 1.21.1 且没有跨版本共享的 Forge 代码，暂空。"
  },
  "neoforge": {
    "loader": "NeoForge",
    "populated": false,
    "note": "加载器层：NeoForge 各版本共用。当前只有 1.21.1 一个 NeoForge 目标，暂空。"
  },
  "fabric": {
    "loader": "Fabric",
    "populated": false,
    "note": "加载器层：Fabric 各版本共用。当前只有 1.21.1 一个 Fabric 目标，暂空。"
  }
}
